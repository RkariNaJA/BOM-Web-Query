# Extractable Components

**Status: GREENFIELD — nothing to extract.**

No existing UI components, so Step 2.5 (component extraction) is skipped per the superdesign SOP
("Brand new projects with no existing UI components").

There is also no shared shell to extract: this app is a single screen with no nav, sidebar, or
footer that repeats across pages. Re-run init after `static/` is built if reusable components are
wanted for future pages.
